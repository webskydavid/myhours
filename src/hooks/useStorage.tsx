import React from 'react';

const useStorage = (key: string) => {
  const getItem = () => {
    return '';
  };

  const setItem = () => {
    return '';
  };

  return { getItem };
};

export default useStorage;
